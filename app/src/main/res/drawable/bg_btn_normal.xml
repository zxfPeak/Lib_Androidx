<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 内容底色 -->
    <solid android:color="#00ffffff" />

    <!-- 边框 -->
    <stroke
        android:width="1dp"
        android:color="@color/bg_common" />

    <!-- 圆角 -->
    <corners android:radius="3dp" />

    <!-- 间距 -->
    <padding
        android:bottom="10dp"
        android:left="14dp"
        android:right="14dp"
        android:top="10dp" />

</shape>